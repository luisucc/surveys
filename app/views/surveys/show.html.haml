.row
  .col-sm-9
    %h2= @survey.name

    %h4 Description:
    %p= @survey.description

    %h4 Questions:

    - @survey.questions.each do |question|
      .well
        .row
          .col-sm-10
            = question.value
          .col-sm-2.text-right
            %span.badge.well-material-blue-400
              = question.type
  .col-sm-3
    -# You can make a tree
= link_to 'Edit', edit_survey_path(@survey)
\|
= link_to 'Back', surveys_path
